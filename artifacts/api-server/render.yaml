services:
  - type: web
    name: golden-brew-api
    runtime: node
    buildCommand: npm install --ignore-scripts && npm run build && npm run migrate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
    autoDeploy: true
